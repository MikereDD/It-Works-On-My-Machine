# feature/convert

No dedicated logic here yet — format conversion and size compression are
covered by `Effects.convert()` and `Effects.compress()`. This package is
reserved for the convert/compress *UI* (format picker, bitrate/sample-rate/
channel selectors, batch list) when it's built out.
