#!/bin/sh
# Gradle wrapper launcher. If gradle/wrapper/gradle-wrapper.jar is missing,
# open this project in Android Studio (it regenerates it) or run `gradle wrapper`.
DIR=$(cd "$(dirname "$0")" && pwd)
exec java -jar "$DIR/gradle/wrapper/gradle-wrapper.jar" "$@"
